[package]
name = "faasta-interface"
version = "0.2.0"
edition = "2024"
description = "bitrpc types for the faasta serverless platform"
license = "MIT"

[dependencies]
bincode = "2.0.1"
bitrpc = "0.4"
bitcode = "0.6.9"
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
