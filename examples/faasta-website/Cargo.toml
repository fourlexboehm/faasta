[workspace]

[package]
name = "faasta-website"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
faasta = { path = "../../faasta" }

[profile.release]
lto = true
strip = "symbols"
