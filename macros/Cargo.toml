[package]
name = "faasta-macros"
version = "0.1.1"
edition = "2021"
license = "MIT"
description = "Proc macros for Faasta"

[dependencies]
quote = "1"
syn = { version = "2", features = ["full", "visit"] }

[lib]
proc-macro = true

